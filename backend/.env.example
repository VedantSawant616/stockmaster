# Database Configuration
DATABASE_URL=sqlite:///./stockmaster.db

# Email Configuration (Optional - for sending real OTP emails)
# If not configured, OTPs will be displayed in the console

# Gmail Configuration (Recommended for Development)
# 1. Enable 2FA: https://myaccount.google.com/security
# 2. Create App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM=your-email@gmail.com

# Alternative: SendGrid (for Production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM=noreply@yourdomain.com

